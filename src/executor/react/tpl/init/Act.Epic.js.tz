import Ux from "ux";
import Types from "./Act.Types";
import {Ajax} from "app";

export default {
    fn#MOD#Inited:Ux.rxFlow(Types.fn#MOD#Inited)
         .bind(Ajax)
         .mount(

         ).to()
}