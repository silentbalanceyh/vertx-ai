package <%= packageName %>;

import io.zerows.boot.VertxApplication;
import io.zerows.epoch.annotations.Up;

@Up
public class <%= name %>App {
    public static void main(final String[] args) {
        VertxApplication.run(<%= name %>App.class);
    }
}
