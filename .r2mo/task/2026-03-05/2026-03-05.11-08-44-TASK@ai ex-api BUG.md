---
runAt: 2026-03-05.10-21-33
title: ai ex-api BUG
---
参考之前的改动，我在目录下执行 ai ex-api 命令，基础输出到 ZERO_MODULE 的是对的，但是我当前目录下应该有我选择角色的 CODE 目录，这个目录下没有生成所选角色的权限文件信息。

测试目录：/Users/lang/zero-cloud/app-zero/r2mo-apps/app-aisz/aisz-app/aisz-app-hotel
非 `e501b47a-c08b-4c83-b12b-95ad82873e96` 角色的输出目录：/Users/lang/zero-cloud/app-zero/r2mo-apps/app-aisz/aisz-app/aisz-app-hotel/src/main/resources/init/oob/role

## Changes

### 2026-03-05 (Commit: e83eeb4)

**修改文件：** `src/commander-ai/fn.ex.api.js`

**BUG 根因：**
非参考角色的输出路径构造错误。代码将路径构造为 `{cwd}/{CODE}/security/RBAC_ROLE/ADMIN.SUPER/`，但实际项目中非参考角色文件的存放位置是 `{cwd}/src/main/resources/init/oob/role/{CODE}/`（与 ZERO_MODULE 输出路径完全不同，是项目自身的初始化数据目录）。

**变更内容：**

1. **修正非参考角色输出路径** (第 702-714 行)
   - 修改前：`{cwd}/{CODE}/security/RBAC_ROLE/ADMIN.SUPER/`（错误路径，构造来自对 ZERO_MODULE 结构的误套用）
   - 修改后：`{cwd}/src/main/resources/init/oob/role/{CODE}/`（正确路径，匹配项目实际目录结构）
   - 同时移除了冗余的两级路径判断（`roleCodeDir` + `targetRoleDir` 双重检查），简化为直接判断最终目录

2. **修正返回值引用** (第 815 行，上次漏改)
   - `outRolePath: "—"` → `outRolePaths: []`（与成功路径保持字段名一致）

**变更统计：** +2 行，-7 行
